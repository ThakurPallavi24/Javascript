//Template Literals:It allows to embed expressions directly within the string using ${} or backtick(``)
let variable = "world";
let greeting = `Hello, ${variable}!`;
console.log(greeting);

let a = 5;
let b = 10;
console.log(`The sum of ${a} and ${b} is ${a + b}.`);
