//Array used to store a collection of values, such as numbers, strings, objects, or even other arrays.
let num = [1, 2, 3, 4, 5, 6];

console.log(num);

console.log(num[0]);

console.log(num[5]);

for (const n of num) {
  console.log(n);
}
